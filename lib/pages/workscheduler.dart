import 'package:flutter/material.dart';

class Workschedulerpage extends StatelessWidget {
  const Workschedulerpage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text("Work Scheduler Page"),
      ),
    );
  }
}
